# 前端请求后端 API 的显式地址（可选）
# - 留空：自动按 host 推断（本机/IP -> :8000；域名 -> 同域名）
# - 有值：强制使用该地址（例如 https://api.example.com）
VITE_API_BASE_URL=

# Vite 开发代理目标（开发环境生效）
# 前端 dev 服务访问 /api 会被代理到该地址
VITE_API_PROXY_TARGET=http://127.0.0.1:8000

# 自动推断到本机/IP 时使用的后端端口
VITE_API_FALLBACK_PORT=8000
