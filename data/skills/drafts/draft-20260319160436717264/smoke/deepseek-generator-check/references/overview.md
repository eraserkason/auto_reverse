# WordPress 逆向辅助概述

本技能树旨在辅助对 WordPress 站点进行初步的逆向工程与功能验证。它不涉及复杂的漏洞利用，而是聚焦于识别和解析站点的公开接口，特别是认证相关的表单。

**核心流程**：
1.  定位 WordPress 站点的登录入口（通常为 `/wp-login.php` 或 `/wp-admin`）。
2.  分析登录页面的 HTML 结构，识别用户名、密码等表单字段。
3.  提取表单的提交目标（action URL）和方法（POST）。

**使用前提**：需要一个可公开访问的 WordPress 站点 URL。